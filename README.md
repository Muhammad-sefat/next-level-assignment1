# What are some differences between interfaces and types in TypeScript?
=> TypeScript কাজ করার সময় আমরা দুইভাবে object-এর গঠন বা data structure define করতে পারি—interface আর type দিয়ে।
দুটোই দেখতে একই রকম হলেও, কিছু ব্যবহারগত পার্থক্য আছে। নিচে খুব সহজ ভাষায় ব্যাখ্যা দিলাম, যাতে যে কেউও সহজে বুঝতে পারে।

**Interface কীভাবে আলাদা ?** <br/>

Interface মূলত object-এর কাঠামো তৈরি করার জন্য ব্যবহার হয়।
যেমন—একটা object-এ কী কী property থাকবে, কোনটা string হবে, কোনটা number—এসব interface দিয়ে খুব সুন্দরভাবে define করা যায়।

Interface-এর একটা মজার বৈশিষ্ট্য হলো, একই নামে একাধিক interface লিখলে এগুলো একসাথে merge হয়ে যায়।
মানে, TypeScript নিজে থেকেই সব property একীভূত করে নেয় — এটা types-এ করা যায় না।

আরও একটা সুবিধা হলো, interface সহজে extend করা যায়।
অনেকটা class extend করার মতো, নতুন property যোগ করা খুব সহজ।

**Type কীভাবে আলাদা ?** <br/>

Type তুলনামূলকভাবে বেশি flexible।
এটা দিয়ে শুধু object structure না—primitive type, union, tuple, function signature—সবই define করা যায়।

Type-এর বিশেষত্ব হলো, এটি complex টাইপ তৈরি করতে খুব সুবিধা দেয়।
যেমন: type A | B, type A & B—এভাবে বিভিন্ন টাইপ একত্রে মিলিয়ে নতুন টাইপ বানানো যায়।

তবে type-এর একটা সীমাবদ্ধতা আছে: একই নামে দুইবার type declare করলে error হবে।
interface-এর মতো merge হবে না।

**সহজভাবে মূল পার্থক্যগুলো**

- interface object structure define করার জন্য বেশি natural।

- type বেশি flexible—object-এর বাইরে আরও অনেক কিছুর টাইপ তৈরি করতে পারে।

- interface সহজে extend করা যায়, এমনকি একই নামে একাধিক interface merge হয়।

- type merge হয় না, তবে intersection (&) দিয়ে combine করা যায়।

- type দিয়ে union, tuple, primitive—সব define করা যায়; interface দিয়ে না।

**⭐ শেষ কথা** <br/>

Type আর interface—দুটোই শক্তিশালী টুল।
অনেক সময় এদের একে অন্যের জায়গায়ও ব্যবহার করা যায়।
কিন্তু যখন object-এর গঠন পরিষ্কারভাবে define করতে হবে, তখন interface বেশি natural লাগে,
আর যখন flexible বা complex টাইপ দরকার হয়, তখন type ভালো কাজ করে।



# What is the use of the keyof keyword in TypeScript? Provide an example.
=> keyof এমন একটি keyword যা কোনো object type-এর সব property নামকে একসাথে একটি union type হিসেবে বের করে দেয়।
এটা দিয়ে আমরা নিশ্চিত করতে পারি যে object-এর property access সবসময় valid key দিয়ে করা হচ্ছে — ভুল key দিলে TypeScript error দেখায়।

**⭐ সহজ Example** <br/>

```
interface Person {
  name: string;
  age: number;
  gender: string;
}

type PersonKeys = keyof Person; 
// এখন PersonKeys হচ্ছে "name" | "age" | "gender"

function getProperty(obj: Person, key: PersonKeys) {
  return obj[key];
}

const person: Person = {
  name: "John",
  age: 25,
  gender: "male",
};

console.log(getProperty(person, "name")); // John
```


**👉 ব্যাখ্যা :** <br/>

- keyof Person বের করে "name" | "age" | "gender"

- তাই getProperty(person, "name") valid

- কিন্তু "email" দিলে সাথে সাথেই error — কারণ সেটা Person-এর key না

**⭐ সংক্ষেপে (Conclusion)** <br/>

keyof আমাদের object-এর সব property নামকে type হিসেবে ব্যবহার করতে দেয়।
এতে কোড আরও নিরাপদ হয়, কারণ ভুল key দিলে TypeScript আগেই error দেখায়।
Object-এর property নিয়ে type-safe ভাবে কাজ করতে keyof খুবই কাজে লাগে।


# Explain the difference between any, unknown, and never types in TypeScript.
=> TypeScript ব্যবহার করলে আমরা ডেটার টাইপ নিয়ে কাজ করি যাতে কোড আরও নিরাপদ থাকে। কিন্তু কিছু বিশেষ জায়গায় আমরা নিশ্চিত থাকতে পারি না কোন টাইপ ব্যবহার করতে হবে—এই সময় TypeScript আমাদের তিনটি বিশেষ টাইপ দেয়: any, unknown, এবং never। এগুলো ঠিকভাবে বুঝলে বাস্তবে কাজ করা অনেক সহজ হয়ে যায়।

**⭐ any** <br/>

any সেই টাইপ যা TypeScript-এর সব নিয়ম একদম বন্ধ করে দেয়। আপনি যেকোনো মান দিতে পারবেন, যেকোনো জায়গায় ব্যবহার করতে পারবেন—TypeScript কোনো অভিযোগ করবে না।

এটা সুবিধাজনক হলেও বড় সমস্যা হলো: ভুল করলেও TypeScript জানায় না। তাই বড় প্রোজেক্টে any বেশি ব্যবহার করা ঝুঁকিপূর্ণ।

**⭐ unknown** <br/>

unknown দেখতে any-এর মতো হলেও এটি অনেক নিরাপদ।
এখানে যেকোনো মান রাখা যায়—কিন্তু TypeScript আপনাকে জোর করে বলে:

“যেটা ব্যবহার করতে যাচ্ছো, আগে নিশ্চিত হও এটা কোন টাইপ।”

অর্থাৎ ব্যবহার করার আগে আপনাকে চেক করতে হবে মানটি string, number, object—কোনটা।
এভাবে unknown আপনার কোডকে ভুল থেকে বাঁচায়।

**⭐ never** <br/>

never ব্যবহার হয় যখন কোনো মান ফেরত দেওয়ার প্রশ্নই আসে না।
যেমন—একটি function শুধু error ছুঁড়ে দিচ্ছে, বা এমন একটি লজিক যেখানে সব সম্ভাবনা আগে থেকেই কভার করা আছে এবং এর বাইরে আর কিছু আসার উপায় নেই।

never basically বোঝায় —
“এখানে কোনো মান থাকতে পারে না।”

এটা ব্যবহার করে আমরা নিশ্চিত হতে পারি যে কোডের সব কেস ঠিকভাবে handle করা হয়েছে।

**⭐ সংক্ষেপে** <br/>

- any সবচেয়ে কম নিরাপদ, কারণ এটা TypeScript-এর নিয়ম বন্ধ করে দেয়।

- unknown নিরাপদভাবে অজানা মান নিয়ে কাজ করতে সাহায্য করে।

- never বোঝায় এমন জায়গা যেখানে কোনো মান বা ফলাফল আসার সম্ভাবনা নেই।

এগুলো সঠিকভাবে ব্যবহার করলে TypeScript কোড আরও পরিষ্কার, নিরাপদ এবং ভুলবিহীন হয়।
